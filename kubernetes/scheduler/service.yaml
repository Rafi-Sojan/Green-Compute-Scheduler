apiVersion: v1
kind: Service
metadata:
  name: green-compute-scheduler
  namespace: green-compute
spec:
  selector:
    app: green-compute-scheduler
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
